---
description: Scan the design system to find what UI components already exist before designing new ones
---

Run the `design-system-check` skill.

Feature or UI context: $ARGUMENTS

If $ARGUMENTS is empty, ask what feature or UI area needs to be checked.
