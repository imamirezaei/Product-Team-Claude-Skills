---
description: Write or review UI copy — button labels, error messages, empty states, tooltips, confirmation dialogs
---

Write or review microcopy for a UI element.

Run the `microcopy-writer` skill.

Element or screen: $ARGUMENTS

If $ARGUMENTS is empty, ask the designer what element needs copy and the context in which it appears.
