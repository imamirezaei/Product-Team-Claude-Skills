---
description: Research existing design patterns, component inventory, and open UX decisions before starting a new feature design
---

Research the design landscape before starting work on a feature.

Run the `design-research` skill.

Feature or screen: $ARGUMENTS

If $ARGUMENTS is empty, ask the designer which feature they are picking up and whether they have a wireframe or PM spec to reference.
