---
description: Analyze the blast radius of a change before release — what else might break or be affected
---

Run the `release-impact` skill.

Change or feature: $ARGUMENTS

If $ARGUMENTS is empty, use the latest git diff to determine what has changed.
