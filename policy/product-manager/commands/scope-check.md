---
description: Identify scope creep in an in-progress feature and find where scope can be trimmed without losing core value
---

Run the `scope-check` skill.

Feature: $ARGUMENTS

If $ARGUMENTS is empty, ask the PM to describe the feature and how its scope has grown.
