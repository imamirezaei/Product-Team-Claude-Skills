---
description: Convert raw message copy into a ready-to-use Telegram Rich Message (InputRichMessage) payload for engineering
---

Run the `telegram-rich-message` skill.

Raw message copy: $ARGUMENTS

If $ARGUMENTS is empty, ask the PM to paste the raw message copy they want to send (and, optionally, the structure intent and whether to use markdown or html).
