---
description: Compare and prioritize features against the current roadmap, then log the decision
---

Compare and prioritize these features.

## Chain

**Step 1 — feature-prioritization**
Run the `feature-prioritization` skill. Score features, make the trade-off explicit, and build a defensible argument.

**Step 2 — decision-logger** (automatic after PM confirms decision)
After the PM confirms the prioritization decision, automatically run the `decision-logger` skill to document the trade-off. Pre-fill all context from Step 1 — do not ask the PM to repeat it.

Features to compare: $ARGUMENTS

If $ARGUMENTS is empty, ask the PM to describe the features or the prioritization decision they need to make.
